{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "exertion-app",
  "scripts": {
    "lint": "lerna run lint --parallel",
    "test": "lerna run test --parallel",
    "dev": "lerna run --stream dev"
  },
  "dependencies": {
    "latest": "^0.2.0"
  }
}
